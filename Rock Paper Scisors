from tkinter import *
import random as ran
import tkinter.font as font

player_score = 0
AI_score = 0
options = [("rock", 0),("paper", 1),("scisors",2)]
def comp_wins():
    global AI_score
    global player_score
    AI_score+=1
    winner_label.config(text = "AI Wins!")
    comp_score_label.config(text = f"AI Score: {AI_score}")
    score_label.config(text = f"Player score: {player_score}")

def player_wins():
    global AI_score
    global player_score
    player_score+=1
    winner_label.config(text = "You Win!")
    comp_score_label.config(text = f"AI Score: {AI_score}")
    score_label.config(text = f"Player score: {player_score}")

def tie():
    global AI_score
    global player_score
    winner_label.config(text = "Its a tie!")
    comp_score_label.config(text = f"AI Score: {AI_score}")
    score_label.config(text = f"Player score: {player_score}")

def comp_choice():
    return ran.choice(options)

def player_choice(player_input):
    pass



root = Tk()
root.geometry("800x500")


Title = root.title("Rock Paper Scisors")
root.config(background="Black")

app_font = font.Font(size = 12)

Game_title = Label(root, text = "Rock Paper Scisors!",
                   background="Black", foreground="Red")

Game_title.pack()
winner_label = Label(root, text="Lets start",
                     font = font.Font(size = 13),
                     pady=8, background="Black",
                     foreground="White")
winner_label.pack()
input_frame = Frame(root,bg = "Yellow")
player_options = Label(input_frame,text="Your options: ",
                       font = app_font,bg="Yellow",fg = "Blue")
player_options.grid(row=0,column=0)

rock_btn = Button(input_frame, text = "Rock", bd=2,bg="Yellow",command = None)
scisors_btn = Button(input_frame, text = "Scisors", bd=2,bg="Yellow",command = None)
paper_btn = Button(input_frame, text = "Paper", bd=2,bg="Yellow",command = None)

rock_btn.grid(row = 1,column = 0,padx=8,pady=8)
scisors_btn.grid(row = 1,column = 1,padx=8,pady=8)
paper_btn.grid(row = 1,column = 2,padx=8,pady=8)

score_label = Label(input_frame,text = "Player Score: ",bg="Yellow",fg="Blue")
score_label.grid(row=3,column = 0)

player_choice_label = Label(input_frame,text="You selected: ",bg="Yellow",fg = "Blue")
player_choice_label.grid(row = 3,column = 1)

comp_choice_label = Label(input_frame,text="AI selected: ",bg="Yellow",fg = "Blue")
comp_choice_label.grid(row = 4,column = 1)

comp_score_label = Label(input_frame,text = "AI Score: ",bg="Yellow",fg="Blue")
comp_score_label.grid(row=4,column = 0)

input_frame.pack()


root.mainloop()
