import pgzrun
import random as ran
WIDTH = 300
HEIGHT = 500
def draw():
    r = 255
    g = 0
    b = ran.randint(0,255)
    radius = 100
    for i in range(10):
        screen.draw.circle((WIDTH/2, HEIGHT/2), radius, (r,g,b))
        r -= 10
        g += 10
        radius -= 5

pgzrun.go()
